<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */

/**
 * Folix Game Theme - Header
 *
 * 基于 Magento 原生 header.phtml 改造
 * PC 端：Top Bar + Main Bar
 * 移动端：简化 Header + 侧边栏
 */
?>
<?php
/**
 * Top bar
 */
?>
<div class="header-container">
    <div class="header panel wrapper">
        <div class="panel header">
            <div class="header-links-left">
                <?= $block->getChildHtml('folix.top.links') ?>
            </div>
            <div class="header-links-right">
                <?= $block->getChildHtml('folix.app.link') ?>
                <?= $block->getChildHtml('store_language') ?>
                <?= $block->getChildHtml('folix.top.auth') ?>
            </div>
        </div>
    </div>

    <?php
    /**
     * Main bar
     */
    ?>
    <div class="header content">
        <div class="header-main-content">
            <!-- Logo -->
            <div class="header-logo">
                <?= $block->getChildHtml('logo') ?>
            </div>

            <!-- Navigation (PC 端) -->
            <nav class="pc-navigation" data-action="navigation">
                <?= $block->getChildHtml('catalog.topnav') ?>
            </nav>

            <!-- Search -->
            <div class="header-search">
                <?= $block->getChildHtml('top.search') ?>
            </div>

            <!-- Minicart -->
            <div class="header-minicart">
                <?= $block->getChildHtml('minicart') ?>
            </div>
        </div>
    </div>

    <?php
    /**
     * Mobile Header (汉堡菜单 + 购物车)
     */
    ?>
    <div class="mobile-header">
        <button type="button" class="nav-toggle" aria-label="<?= $block->escapeHtmlAttr(__('Menu')) ?>" data-action="toggle-nav">
            <span></span>
            <span></span>
            <span></span>
        </button>
        <div class="mobile-minicart">
            <?= $block->getChildHtml('minicart') ?>
        </div>
    </div>
</div>
