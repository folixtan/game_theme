<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */

/** @var Magento\Customer\Block\Account\Customer $block */
?>
<?php if ($block->customerLoggedIn()) : ?>
    <li class="customer-welcome folix-customer-welcome">
        <span class="customer-name"
              role="link"
              tabindex="0"
              data-mage-init='{"dropdown":{}}'
              data-toggle="dropdown"
              data-trigger-keypress-button="true"
              data-bind="scope: 'customer'">
            <button type="button"
                    class="action switch folix-customer-btn"
                    tabindex="-1"
                    data-action="customer-menu-toggle">
                <span><?= $block->escapeHtml(__('Change')) ?></span>
                <svg class="folix-dropdown-icon" width="12" height="12" viewBox="0 0 12 12">
                    <path d="M6 8L1 3h10L6 8z" fill="currentColor"/>
                </svg>
            </button>
        </span>
        <!-- 用户下拉菜单 - 使用原生 dropdown -->
        <div class="customer-menu folix-user-dropdown" data-target="dropdown">
            <div class="user-dropdown-trigger">
                <div class="avatar"><?= mb_strtoupper(mb_substr($block->escapeHtml($block->getCustomerName()), 0, 2)) ?></div>
                <span class="username"><?= $block->escapeHtml($block->getCustomerName()) ?></span>
                <span class="arrow">▼</span>
            </div>
            <div class="user-dropdown-menu">
                <a href="<?= $block->escapeUrl($block->getUrl('customer/account')) ?>">
                    <span><?= $block->escapeHtml(__('My Account')) ?></span>
                </a>
                <a href="<?= $block->escapeUrl($block->getUrl('sales/order/history')) ?>">
                    <span><?= $block->escapeHtml(__('My Orders')) ?></span>
                </a>
                <a href="<?= $block->escapeUrl($block->getUrl('wishlist')) ?>">
                    <span><?= $block->escapeHtml(__('My Wishlist')) ?></span>
                </a>
                <div class="divider"></div>
                <a href="<?= $block->escapeUrl($block->getUrl('customer/account/logout')) ?>" class="logout">
                    <span><?= $block->escapeHtml(__('Sign Out')) ?></span>
                </a>
            </div>
        </div>
        <script type="text/x-magento-init">
        {
            "*": {
                "Magento_Ui/js/core/app": {
                    "components": {
                        "customer": {
                            "component": "Magento_Customer/js/view/customer"
                        }
                    }
                }
            }
        }
        </script>
    </li>
<?php endif; ?>
